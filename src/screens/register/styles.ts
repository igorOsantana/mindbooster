import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  paddingSide: {
    paddingHorizontal: 40,
  },
  title: {
    marginVertical: 70,
    fontSize: 28,
    fontWeight: 'bold',
    color: '#fff',
  },
  input: {
    marginBottom: 20,
  },
});
